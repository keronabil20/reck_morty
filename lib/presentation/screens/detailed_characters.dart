import 'package:flutter/material.dart';

class DetailedCharacters extends StatelessWidget {
  const DetailedCharacters({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}